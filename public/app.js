const tg=window.Telegram?.WebApp;if(tg){tg.ready();tg.expand()}const $=id=>document.getElementById(id);const state={squad:null};function api(path){const headers={};if(tg?.initData)headers['X-Telegram-Init-Data']=tg.initData;return fetch(path,{headers}).then(async r=>{const text=await r.text();let data={};try{data=text?JSON.parse(text):{}}catch{}if(!r.ok)throw Object.assign(new Error(data.error||`HTTP ${r.status}`),{status:r.status,data});return data})}function showPage(page){document.querySelectorAll('.page').forEach(x=>x.classList.toggle('active',x.dataset.page===page));document.querySelectorAll('.nav-item').forEach(x=>x.classList.toggle('active',x.dataset.go===page));if(page==='squad')loadSquad()}function renderSquad(data){state.squad=data;if(!data.inSquad){$('members').textContent='0';$('active').textContent='0';$('activity').textContent='0%';$('squadMembers').textContent='0';$('squadActive').textContent='0';$('squadActivity').textContent='0%';$('goals').innerHTML='<article class="info-card"><strong>No squad yet</strong><p>Your account is not currently assigned to a squad.</p></article>';return}const s=data.squad||{};const members=Number(s.memberCount||0),active=Number(s.activeMemberCount||0),activity=Number(s.activityPercent||0);$('members').textContent=members.toLocaleString();$('active').textContent=active.toLocaleString();$('activity').textContent=`${activity}%`;$('squadMembers').textContent=members.toLocaleString();$('squadActive').textContent=active.toLocaleString();$('squadActivity').textContent=`${activity}%`}async function loadSquad(){try{const data=await api('/api/squad');renderSquad(data);loadGoals()}catch(error){if(error.status===401){$('accountText').textContent='Open DzMoney inside Telegram to authenticate your account.';return}$('accountText').textContent='The Squad service is temporarily unavailable.'}}async function loadGoals(){try{const data=await api('/api/squad/goals');if(!data.inSquad||!data.goals?.length){$('goals').innerHTML='<article class="info-card"><strong>Squad goals</strong><p>No active goals are published for your squad.</p></article>';return}$('goals').innerHTML=data.goals.map(g=>{const progress=Math.min(100,(Number(g.progress||0)/Math.max(1,Number(g.target_quantity||1)))*100);return `<article class="goal-card"><div class="goal-top"><span>${escapeHtml(g.target_type||'Goal')}</span><strong>${escapeHtml(g.title||'Squad goal')}</strong></div><p>${escapeHtml(g.description||'Contribute qualifying activity to this goal.')}</p><div class="progress"><i style="width:${progress}%"></i></div><div class="goal-meta"><span>${Number(g.progress||0).toLocaleString()} / ${Number(g.target_quantity||0).toLocaleString()}</span><span>${progress.toFixed(0)}%</span></div></article>`}).join('')}catch{$('goals').innerHTML='<article class="info-card"><strong>Squad goals</strong><p>Goals are temporarily unavailable.</p></article>'}}function escapeHtml(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}document.addEventListener('click',e=>{const nav=e.target.closest('[data-go]');if(nav)showPage(nav.dataset.go)});const user=tg?.initDataUnsafe?.user;if(user){$('welcomeLabel').textContent=`WELCOME, ${String(user.first_name||'').toUpperCase()}`;$('accountText').textContent=`Connected as ${user.first_name||''}${user.last_name?` ${user.last_name}`:''}.`};loadSquad();
