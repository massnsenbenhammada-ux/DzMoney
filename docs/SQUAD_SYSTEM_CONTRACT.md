# DzMoney — Squad System Contract

> **Status: SUPERSEDED / HISTORICAL CONTRACT**
>
> The redesigned Squad business/design source of truth is `docs/SQUAD_SYSTEM_MASTER_CONTRACT.md`.
>
> This file is retained as a historical lineage marker only. Do not use it as current authority and do not reintroduce rules from it when they conflict with the master contract.

## Authority rule

For the redesigned Squad model, consult `docs/SQUAD_SYSTEM_MASTER_CONTRACT.md`.

The historical contract remains recoverable through Git history. Implemented runtime behavior is verified against the actual implementation, database schema, migrations, tests, and CI; these do not silently redefine the locked future-phase contract.

## Reconciliation note

This file previously contained the pre-reconciliation Squad contract, including historical daily activation, modifier, Challenge, membership, and tier rules. Those rules are no longer current authority for the redesigned Squad model.

No runtime code, database schema, Economy/Ledger, Reward, Verification, TON, or API behavior is changed by this documentation reconciliation.
