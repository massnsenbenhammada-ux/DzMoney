# DzMoney 2.0 — MASTER ROADMAP

> **Single source of truth for the clean rebuild.** This roadmap replaces all previous roadmaps and old BUX-era assumptions. The project uses a new PostgreSQL database and must not reintroduce legacy BUX/core/compatibility business logic.

## Global implementation rules

- Backend/database is the source of truth; frontend never calculates authoritative balances or rewards.
- Every balance movement is atomic, idempotent and recorded in an auditable ledger.
- Admin settings must change real backend business rules, not only displayed values.
- Referral, Squad and Reward Pool are completely independent systems.
- Build and test one subsystem at a time.
- Do not carry obsolete BUX names, legacy fields or unexplained settings into the new system.
- Keep modules isolated so fixing one subsystem does not destabilize unrelated subsystems.
- **TON is an external reference/settlement currency only. The internal DzMoney economic reward/distribution currency is DZX.**

---

# Phase 1 — Economy & Currency Core 🔒 SPECIFICATION COMPLETE

## Currencies

### COIN
Activity/reward currency. Earned from qualifying activity such as tasks and advertisements and from applicable referral or promotional rewards.

### DZX
**Main internal economic currency.** DZX is the currency used for economic rewards, Reward Pool distributions, applicable Promo rewards, deposits after TON conversion, campaign budgets and withdrawal accounting.

**Agreed DZX source model:**
- advertisements;
- tasks;
- referral earnings;
- Reward Pool distributions;
- deposits converted from TON;
- Promo rewards when the configured Promo reward is DZX.

**Squad is NOT a direct DZX source.** Squad only modifies an otherwise qualifying base activity reward by the configured percentage. The resulting reward must retain its original economic source and record the Squad modifier separately.

### DZP
**Package currency and activity indicator.** DZP is used to buy packages and **earned activity DZP** is used to measure activity/Reward Pool weight.

### TON
External blockchain currency. TON is **not** an internal DzMoney wallet currency and is **not** the Reward Pool payout unit. It is used only as the external value/reference for deposits and withdrawals.

## Fixed economic relationship

**1 TON = 10,000 DZX = 10,000,000 COIN**

TON is displayed as the external reference value of the DZX economy. The system must avoid using TON as an internal reward balance or Reward Pool distribution unit.

Therefore:

**1 DZX = 1,000 COIN**

## DZP conversion rates

**1,000 COIN = 1 DZP**

**10,000 COIN = 10 DZP**

**10 DZX = 1 DZP**

**100 DZX = 10 DZP**

Therefore:

**1 DZP = 10,000 COIN = 10 DZX**

## Conversion direction

Allowed:
- COIN → DZP
- DZX → DZP

Not allowed:
- DZP → COIN
- DZP → DZX

## DZP source separation

The ledger must distinguish at least:
- `earned_dzp` — obtained from qualifying activity;
- `converted_dzp` — obtained through COIN/DZX conversion;
- `purchased_dzp` — obtained through purchase/package-related acquisition.

**Converted DZP and purchased DZP never increase Reward Pool weight.**

This rule must be shown clearly in the user's Conversion area and enforced by the backend.

## DZX source separation

The ledger must identify DZX by its true source. The complete agreed DZX source set is:
- advertisements;
- tasks;
- referral earnings;
- Reward Pool distributions;
- deposits converted from TON;
- Promo rewards when the configured Promo reward is DZX.

Promo rewards may also be COIN. The reward currency is determined by the configured Promo campaign and must be recorded explicitly in the ledger.

**Squad is a modifier, not a source.** A Squad percentage can increase the reward generated by an eligible base activity, but it must never create an independent DZX source, be counted as a separate earning stream, or be included in Referral lifetime calculations as if it were base activity.

Source metadata must remain immutable in the ledger. A reward must retain its original source even when a Squad modifier changes its amount.

**Important:** DZX received from deposit/transfer does not become earned activity and must not be used to inflate Reward Pool weight. Promo DZX is a distinct `promo` source. Reward Pool DZX is a distinct `reward_pool` source. Squad modifier metadata must remain separate from source attribution.

## Admin economy

Admin controls the configurable economic rates/settings. Changes affect the real backend/database logic. Historical transactions retain the rates that applied when they occurred; changing a rate must not rewrite past ledger records.

---

# Phase 2 — Activity, Ads & Tasks

## Standard default reward

**1,000 COIN + 1 DZX + 1 DZP**

This is the default reward for qualifying standard task/ad activity and is Admin-configurable.

**Squad modifier:** when the user's Squad conditions are satisfied, the configured Squad percentage modifies the applicable base reward. Squad does not create a new source; it only changes the reward amount produced by the original qualifying activity.

Only the **earned activity DZP** generated by qualifying activity contributes to Daily Activity and Reward Pool weighting.

## Verification contract for tasks

Every non-advertisement task in all task categories uses two explicit user actions:

1. **Execute** — performs the task/action required by the task.
2. **Verify** — submits the completion for server-side verification and reward eligibility.

The **Verify** action is advertisement-gated where configured by the task system: pressing Verify launches a short verification advertisement of **5 or 10 seconds**. The verification advertisement is part of the verification flow and **does not grant an additional task reward** and does not create a second independent activity reward.

The reward is issued only after the backend verifies the task completion and the verification-ad requirement. Verification must be idempotent and cannot be used to claim the same task reward twice.

This Execute → Verify model applies to:
- Daily Tasks;
- Game Tasks;
- Social Tasks;
- Web Tasks;
- Special/Partner Tasks.

**Advertisement tasks are excluded from this two-button task model** because their own completion is the advertisement itself.

## Advertisements

Implement verified advertisement completion, reward idempotency, anti-duplicate protection and server-side reward issuance.

Separate:
- Tasks-page advertisements;
- Reward Pool advertisements;
- verification advertisements attached to task verification.

Reward Pool ads must never be counted as Task ads and vice versa. A verification ad must not be silently counted as a second task reward source.

## Daily Check-in

Daily Check-in is advertisement-gated where configured: the user must complete the required advertisement before receiving the reward.

## Task categories

Strict category separation:
- Daily Tasks
- Game Tasks
- Social Tasks
- Web Tasks
- Special/Partner Tasks

A task cannot be created or validated under an incompatible category.

## User-created tasks

Available to normal users:
- Game
- Social
- Web

Partner/Special campaigns require contacting Admin.

Creator pays the task campaign cost.

Reference pricing:

**1,000 valid visits/executions = 0.90 TON = 9,000 DZX**

The pricing model scales from this reference and is Admin-configurable.

Task lifecycle:

Draft → Pending Review → Active → Paused → Completed/Expired → Closed/Refunded

---

# Phase 3 — Referral System

Referral is a separate one-level lifetime system and is independent from Squad and Reward Pool.

## Qualification

A referred user qualifies after entering through the referral link and completing at least one qualifying activity:
- one verified advertisement; OR
- one verified task.

## Activation reward

**10,000 COIN + 10 DZX + 10 DZP**

The 10 DZP is granted once only at referral qualification.

## Lifetime earnings

Referrer receives **20%** from the referred user's qualifying daily base activity rewards.

The 20% applies only to the referred user's qualifying:
- task activity;
- advertisement activity.

It does NOT apply to:
- Squad modifiers/bonuses;
- Reward Pool distributions;
- Promo rewards;
- package multipliers;
- referral earnings themselves;
- other non-base activity income.

The one-time 10 DZP activation reward is not included in the lifetime 20% calculation. The lifetime calculation uses the applicable COIN + DZX activity reward portion before any Squad modifier.

---

# Phase 4 — Hierarchical Squad System

Squad is completely separate from Referral.

## Hierarchy

Membership follows a tree/hierarchical structure. A newly qualified member is automatically attached to the Squad hierarchy of the member through whom the structure was formed.

Example:

```text
A ─── B ─── C
│          │
D ─── Y    X
```

B, C, D, Y and X can belong to A's hierarchical Squad according to the tree relationship.

No artificial global Squad member cap.

## Levels

- 10 levels.
- Admin defines the required member count for each level.
- Admin defines the bonus percentage for each level.
- Bonus is a percentage increase applied to an eligible base activity reward.
- Each level may have a different percentage.

## Daily activation

For day D, the system checks two conditions:

1. Required Squad member count for the relevant level is satisfied.
2. At least **50% of eligible Squad members** are active that day, where activity means at least one verified advertisement OR one verified task.

If and only if both conditions are satisfied, the Squad modifier is activated for **the following day (D+1)**.

If either condition fails, the modifier is inactive for D+1.

**Squad never becomes a separate DZX source.** It only modifies the amount of an eligible base activity reward. The underlying reward remains attributed to its original source (`advertisement` or `task`, for example), with the Squad percentage stored as modifier metadata.

Squad modifiers never enter the Referral 20% calculation as a separate earning stream.

---

# Phase 5 — Reward Pool

Reward Pool is independent from Referral and Squad.

## Funding

Reward Pool is funded from daily project revenue.

Admin sets:

**Daily Reward Pool Distribution Amount (DZX)**

This is the **DZX amount** distributed to eligible users each day.

TON is only shown as an external reference value. It is not used as the Reward Pool payout unit.

## User activation

The only activation condition is:

**Watch 10 advertisements inside the Reward Pool page.**

Important:
- the 10 ads must be Reward Pool page ads;
- Task ads do not count;
- verification ads do not count as Reward Pool ads;
- before completing the 10 ads, Reward Pool is locked for that user.

The Reward Pool ads use the same standard activity reward as Tasks unless Admin changes the configured default.

## Weight

Daily eligible activity is represented by **earned activity DZP only**.

Example:
- total eligible activity = 25,000 DZP;
- user = 250 DZP;
- base weight = 250 / 25,000 = 1%.

If the user has an active 1.10x package:

**effective weight = 1% × 1.10 = 1.10%**

Purchased, deposited, transferred or converted DZP does not increase weight.

DZX from Promo, Referral or Reward Pool itself is not reclassified as activity DZP and does not retroactively increase the user's activity weight.

Squad modifiers do not create independent Reward Pool weight. Only the **earned activity DZP actually generated by qualifying base activity** contributes to activity weight according to the finalized activity rules.

## Distribution

At the configured daily distribution time:

**User Share (DZX) = Daily Reward Pool DZX × (User Effective Weight / Sum of all eligible Effective Weights)**

Distribution begins at the start of each day at **UTC+1** using the finalized previous-day activity boundary.

The system must create an auditable Reward Pool distribution record for every DZX payout.

### TON reference display

Where the interface shows the value of a Reward Pool distribution in TON, it is a calculated reference only:

**TON reference = DZX amount / 10,000**

No TON is credited to the user's internal Reward Pool balance.

## User explanation

Reward Pool page must explain:
- what Reward Pool is;
- why it is locked;
- the 10-ad activation requirement;
- that only Reward Pool-page ads count;
- that verification ads do not count;
- how earned DZP affects weight;
- that converted/purchased/deposited/transferred DZP does not increase weight;
- that packages multiply weight;
- that payouts are **DZX**;
- that TON is shown only as an external value reference;
- the daily distribution amount;
- that Referral, Promo and Reward Pool DZX are separate economic sources;
- that Squad is a reward modifier and not a separate DZX source.

Include a concise anti-manipulation warning.

---

# Phase 6 — Packages

Packages are displayed on Home.

Only **one active package** can exist per user. No stacking and no new package purchase while another package is active.

When a package expires, the user becomes a normal activity-weight user again.

## Durations

- 30 days
- 60 days
- 90 days
- 180 days
- 360 days
- Lifetime

## Admin controls

- name
- price in DZP
- duration
- multiplier
- availability
- display order
- status
- package color

Each package has a distinct professional name/color.

Optional visual feature: active package color can be reflected in the user's profile until expiry.

Package multiplier affects Reward Pool weight only.

---

# Phase 7 — Buying Points & Conversion UI

Users can buy DZX using **TON only** through the configured purchase/deposit flow.

Admin defines prices, limits and available purchase options.

Purchase/deposit-derived balances must be separated from earned activity balances.

Conversion UI must support:
- COIN → DZP
- DZX → DZP

It must explicitly warn users that converted/purchased/transferred DZP does not increase Reward Pool weight.

---

# Phase 8 — Deposit System

External deposit currency: **TON**.

TON is not kept as an internal reward balance.

Flow:

**TON deposit → blockchain confirmation → automatic TON→DZX conversion → DZX credit**

Initial economic conversion:

**1 TON → 10,000 DZX**

The deposited TON itself is not credited as TON inside the application; the user's internal balance receives DZX after confirmation.

Deposited DZX is tracked separately from earned DZX and must follow the internal withdrawability rules.

Every deposit requires:
- unique blockchain transaction reference;
- confirmation status;
- idempotency protection;
- immutable ledger entry;
- exact TON amount and resulting DZX amount recorded for audit;
- source marked as `deposit`.

---

# Phase 9 — Withdrawal System

Initial minimum withdrawal value:

**0.2 TON**

TON is the external settlement value. Internally, the withdrawal requirement is represented by DZX/COIN economics.

Initial eligibility requirements:

**2,000,000 COIN + 2,000 DZX**

These are configurable by Admin.

At withdrawal:

1. Verify the user satisfies the COIN and withdrawable DZX requirements.
2. Deduct **2,000,000 COIN + 2,000 DZX** atomically.
3. Treat the deducted economic value as the configured **0.2 TON** withdrawal value.
4. Send the external TON settlement to the user's wallet.

Thus the user sees the external withdrawal as **0.2 TON**, while the internal accounting is COIN + DZX.

Withdrawal fees, if enabled, are charged to the user and are Admin-configurable.

Non-withdrawable/deposit-locked DZX cannot satisfy withdrawable DZX requirements.

Withdrawal processing must be atomic, idempotent and ledger-backed.

---

# Phase 10 — Promo Codes

Home includes a Promo Code field.

Flow:

**Enter code → Claim/Redeem → verified advertisement → reward**

The reward is granted only after verified ad completion.

Admin controls code, reward currency, reward amount, limits, expiry, eligibility and ad-gating.

A Promo campaign may reward **COIN or DZX** depending on its configured reward type. Promo DZX is recorded as a distinct `promo` source and is never silently treated as advertisement/task-earned DZX.

---

# Phase 11 — User App UI

## Home

Show:
- COIN balance
- DZX balance
- DZP balance
- Promo Code
- Squad summary
- Squad member count
- Squad level
- Reward Pool status
- Package section
- Daily Activity DZP
- Daily Total Activity
- Coming Soon

Bottom navigation:

**Home — Tasks — Referral — Wallet**

Packages remain on Home.

## User drawer

A right-to-left drawer/modal covering approximately 85% of the screen.

Show:
- username
- profile photo
- three balances
- Squad membership/status
- Reward Pool status
- Daily Activity DZP
- Daily Total Activity

## Conversion explanation

The Conversion area must clearly state:

**Converted or purchased/transferred DZP is not earned activity and does not increase Reward Pool weight.**

## Squad page

Explain hierarchy, member count, level, requirements, 50% activity condition, next level and next-day activation, plus anti-manipulation warning.

## Reward Pool page

Explain activation, 10 Reward Pool ads, DZP weighting, package multiplier, **DZX distribution**, TON reference value and exclusions.

## Task UI

Every non-advertisement task displays two distinct actions:

**Execute** and **Verify**.

Verify launches the configured short verification advertisement (**5 or 10 seconds**) before the backend accepts the completion for reward issuance. Advertisement tasks themselves do not use this two-button flow.

---

# Phase 12 — Admin Panel

Admin Panel is an operational control system, not a decorative interface.

Required sections:

### Dashboard
Real-time:
- total members;
- advertisements watched;
- tasks completed.

Seven-day **bar charts**, not line/curve charts, with different colors for the three metrics.

Bottom lists:
- Top 10 most active members;
- Top 10 members who brought the most referrals.

### Economy
- TON→DZX reference rate
- COIN→DZP
- DZX→DZP
- activity reward defaults
- purchase prices
- conversion settings
- withdrawal economic value

### Users
- search
- user profile
- balances
- account status
- manual balance adjustments with mandatory reason/audit
- explicit DZP source separation
- explicit DZX source separation

### Referral
- qualification
- activation reward
- lifetime percentage

### Squad
- 10 level member requirements
- 10 bonus percentages
- 50% activity requirement
- modifier behavior and source-attribution rules

### Reward Pool
- daily DZX distribution amount
- 10-ad activation requirement
- weighting settings
- package multipliers
- TON reference display rate

### Packages
- six durations
- names
- colors
- prices
- multipliers
- availability/status

### Tasks
- category management
- default rewards
- Execute/Verify behavior
- verification advertisement duration: 5 or 10 seconds
- ad requirements
- user-created task controls
- campaign fee reference
- review/approval
- Partner/Special workflow

### Promo
- promo code
- reward currency: COIN or DZX
- reward amount
- limits
- expiry
- eligibility
- advertisement gating

### Wallet
- deposit configuration
- TON→DZX conversion rate
- withdrawal minimum TON reference value
- COIN requirement
- DZX requirement
- withdrawal fees

No obsolete BUX terminology. No unexplained empty legacy fields.

---

# Phase 13 — Ledger, Security & Anti-Fraud

All financial/activity events must be auditable.

Separate accounting for:
- earned COIN;
- earned DZX;
- purchased/deposited DZX;
- transferred DZX;
- withdrawable/locked DZX;
- earned DZP;
- converted DZP;
- purchased DZP;
- referral rewards;
- Promo COIN/DZX rewards;
- Reward Pool DZX;
- base activity reward amounts;
- Squad modifier percentages and their source activity references;
- package purchases;
- task campaign budgets;
- fees;
- deposits;
- withdrawals;
- TON external settlement references.

Implement:
- atomic transactions;
- idempotency keys;
- duplicate reward protection;
- server-side verification;
- balance invariants;
- audit logs;
- anti-manipulation controls;
- immutable source attribution;
- immutable modifier attribution.

---

# Phase 14 — Testing & Release

Before production:

1. Economy conversion tests.
2. Activity reward tests.
3. Advertisement verification tests.
4. Execute → Verify task-flow tests for every non-ad task category.
5. Verification-ad 5/10-second gating tests.
6. Task-category isolation tests.
7. Referral qualification/lifetime tests.
8. Squad hierarchy and next-day activation tests.
9. Squad modifier/source-attribution tests.
10. Reward Pool 10-ad activation tests.
11. Reward Pool DZX weight/distribution tests.
12. Package single-active/multiplier/expiry tests.
13. Deposit TON→DZX conversion and idempotency tests.
14. Withdrawal eligibility/deduction/0.2 TON settlement tests.
15. Promo COIN/DZX reward and ad-gating tests.
16. Admin setting propagation tests.
17. Dashboard real-time metric tests.
18. Security and anti-fraud tests.
19. End-to-end Telegram Mini App tests.

Only after these pass should production deployment be considered complete.

---

# Current implementation order

**1. Economy & Currency Core → 2. Activity/Ads/Tasks → 3. Referral → 4. Squad → 5. Reward Pool → 6. Packages → 7. Buying Points/Conversions → 8. Deposit → 9. Withdrawal → 10. Promo Codes → 11. User UI → 12. Admin Panel → 13. Ledger/Security hardening → 14. Final testing/release.**

The execution log must be updated after each completed phase. No phase should be marked complete unless its backend behavior and tests are actually implemented.
